// use to colorify the logs;

const chalk = require('chalk');

const red = chalk.red;
const blue = chalk.blue;
const yellow = chalk.yellow;
const green = chalk.green;

const colorify = { red, blue, yellow, green }

module.exports = colorify;